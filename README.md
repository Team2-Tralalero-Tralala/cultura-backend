# 🚀 Cultura Backend

ระบบ Backend สำหรับโปรเจกต์ Cultura พัฒนาโดยใช้ **Node.js**, **Express** และ **Prisma**

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

โครงสร้างหลักภายในโฟลเดอร์ `src`:

- **Controllers**: จัดการ Logic การทำงานของแต่ละ Request แยกตามโมดูล
- **Services**: Business Logic ของระบบ แยกการทำงานออกจาก Controller
- **Routes**: กำหนด Endpoint API และเชื่อมโยงไปยัง Controller ที่เกี่ยวข้อง
- **Middlewares**: Middleware สำหรับจัดการ Request, Validation และ Authentication
- **Libs**: Utility functions และ Library เสริมต่างๆ
- **prisma**: Schema และ Migration ของฐานข้อมูล

---

## ✅ สิ่งที่ต้องเตรียม (Prerequisites)

ก่อนเริ่มใช้งานโปรเจกต์ ตรวจสอบให้แน่ใจว่าติดตั้งสิ่งเหล่านี้แล้ว:

- **Node.js**: แนะนำเวอร์ชัน LTS
- **MySQL**: ฐานข้อมูลที่ใช้ในระบบ

---

## ⚙️ การติดตั้ง (Installation)

1. Clone โปรเจกต์:

   ```bash
   git clone https://github.com/Team2-Tralalero-Tralala/cultura-backend
   cd cultura-backend
   ```

2. ติดตั้ง Dependencies:

   ```bash
   npm install
   ```

3. สร้างไฟล์ `.env`:
   คัดลอกไฟล์ `.env.development` ไปเป็น `.env` และแก้ไขค่า config ให้เหมาะสมกับเครื่องของคุณ (โดยเฉพาะส่วน Database)
   ```bash
   cp .env.development .env
   ```

---

## 🗄️ การจัดการฐานข้อมูล (Database)

โปรเจกต์นี้ใช้ Prisma ORM คำสั่งที่ใช้บ่อย:

- **สร้าง Prisma Client**: (จำเป็นต้องรันทุกครั้งที่มีการเปลี่ยนแปลง Schema)

  ```bash
  npm run generate
  ```

  หรือ `npx prisma generate`

- **Migrate ฐานข้อมูล**: (ใช้อัปเดตโครงสร้าง Database ใน Local dev)

  ```bash
  npm run migrate
  ```

  หรือ `npx prisma migrate dev`

- **Seed ข้อมูลตัวอย่าง**:
  ```bash
  npm run seed
  ```

---

## 🚀 การรันโปรเจกต์ (Running the Project)

### โหมด Development

รันเซิร์ฟเวอร์ด้วย `nodemon` (Restart อัตโนมัติเมื่อแก้ไฟล์):

```bash
npm run dev
```
